node_modules/
.next/
# Harvested and generated seed data; regenerate rather than reformat.
services/api/seed/catalog_snapshot.json
services/api/seed/demo_registries.json
package-lock.json

# Hand-written prose. Hebrew copy, RTL-aware tables and deliberate line breaks
# that Prettier's markdown printer rewrites. Reviewed by reading, not by a
# formatter; README stays formatted because it is mostly structure.
docs/
.cursor/
